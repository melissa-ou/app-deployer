# app-deployer app config file
---
# App inventory
app-inventory:
  # Type of inventory - supported types: file, mysql
  type: file
  # Inventory file - only required for type == file
  file:
# Ansible settings
ansible:
  path:
# Host settings
hosts:
  app-root: $HOME/apps
...
